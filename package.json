{
  "name": "@permutize/smart-accounts",
  "description": "",
  "version": "1.0.0",
  "author": {
    "name": "Permutize Org",
    "url": "https://github.com/Permutize"
  },
  "devDependencies": {
    "@lcov-viewer/cli": "^1.3.0",
    "@types/bun": "^1.2.8",
    "dotenv": "^17.2.3",
    "husky": "^9.1.7",
    "inquirer": "^12.9.6",
    "lcov-badge-generator": "^1.0.5",
    "lint-staged": "^16.2.6",
    "ora": "^9.0.0",
    "viem": "^2.38.0"
  },
  "keywords": [
    "ethereum",
    "foundry",
    "smart-contracts",
    "solidity",
    "typescript"
  ],
  "private": true,
  "lint-staged": {
    "**/*.sol": [
      "forge fmt"
    ]
  },
  "scripts": {
    "badge": "lcov-badge-generator ./coverage/lcov.info -o coverage/badge.svg",
    "postinstall": "forge soldeer install",
    "remappings": "forge remappings > remappings.txt",
    "clean": "rm -rf cache out",
    "build": "forge build",
    "test": "forge test",
    "test:coverage": "forge coverage",
    "test:coverage:report": "forge coverage --report lcov && mv lcov.info ./coverage/lcov.info && lcov-viewer lcov -o ./coverage ./coverage/lcov.info",
    "prepare": "husky"
  }
}
